let n
const vetor = []

n = Number(prompt("Digita a sequencia de N!"))

for(let i = 0; i < n; i++){
    veot[i] = Number(parseFloat("DIgite o número para a posição : " + i))
}

console.log(vetor)
for (let i = vetor.length - 1; i >= 0; i--){
  console.log(vetor[i])
}